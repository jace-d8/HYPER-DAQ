# HYPER-DAQ dependencies.
#
# Install into the virtual environment (venv) with:
#     pip install -r requirements.txt

# --- GUI + data handling ---
dearpygui>=1.10           # the live plot GUI
pandas>=2.0               # in-memory data frame and CSV/JSON/XLSX export
numpy>=1.24               # shared-memory ring-buffer views, math
openpyxl>=3.1             # pandas XLSX backend (used by Save As → .xlsx)

# --- Sensor drivers (always required by the current SENSOR_SPECS) ---
pyserial>=3.5             # Lakeshore 218 (serial)
lakeshore>=1.6            # Lakeshore 336
alicat>=0.6               # Alicat flow controller

# --- NI-DAQ (active on machines with NI hardware) ---
nidaqmx>=1.0

# --- One-time setup tooling ---
Pillow>=10.0              # For the app setup
